# Corwin W. Marsh

Source for my personal site.

This repo is intentionally minimal and mostly maintained for me, but you're welcome to look around.

## Commands

I use `vite+` via `vp` commands here:

```bash
vp install
vp dev
vp build
```

If you're not using `vite+`, use the equivalent `pnpm` scripts instead.
